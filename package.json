{
  "name": "susangat",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "deploy-surge": "npm run build && npx surge dist susangat.surge.sh"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.4.2"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "lenis": "^1.3.21"
  }
}
